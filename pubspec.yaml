name: secure_chat_app
description: "Flolk - Secure Chat Application"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core e Servizi
  firebase_core: ^2.24.0
  cloud_firestore: ^4.14.0
  firebase_storage: ^11.5.6

  # Gestione File e Immagini
  image_picker: ^1.0.7
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  
  # AGGIUNTO: Pacchetto per la generazione automatica delle icone
  flutter_launcher_icons: ^0.13.1

# CONFIGURAZIONE ICONA: Dice al pacchetto quale immagine usare
flutter_launcher_icons:
  android: "launcher_icon"
  image_path: "assets/icon/app_icon.png" # Assicurati che la tua immagine sia in questo percorso o cambialo con quello corretto
  min_sdk_android: 21

flutter:
  uses-material-design: true
  
  # Ricordati di registrare la cartella degli asset qui sotto per includere l'immagine dell'icona
  assets:
    - assets/icon/
